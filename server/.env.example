# Server (端口 3001，避免和其他项目冲突)
PORT=3001

# Database (独立数据库 mira_db)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mira_db
DB_USER=mira
DB_PASSWORD=your_password_here

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# CORS (部署后改成实际域名)
CORS_ORIGIN=http://localhost:5173
